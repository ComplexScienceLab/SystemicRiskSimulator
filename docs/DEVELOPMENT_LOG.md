# SystemicRiskSimulator 开发日志（补全版）

> 本文档根据仓库 git 历史（以当前工作区为准）自动梳理与人工归纳而来，用于回答：**什么时候做了什么、为什么做、影响了哪些模块**。
>
> 维护建议：后续每个版本发布（或每次重要重构）都在本文末尾追加一条「里程碑」记录。

## 0. 生成方法与说明

### 0.1 数据来源

- `git log --reverse --date=short --pretty=format:"%ad|%h|%s"`
- 重点时间段：2024-12 ~ 2026-01（Python 版模拟器与 RL 流程迭代期）

### 0.2 归档策略（可持续维护）

本文采用：

- **按时间段分节**（便于追溯）
- 每节内按“主题”汇总（实验框架、RL、可视化、实验管理、工具/重构、示例与打包）
- 对提交信息过于笼统的（如 `mod 2025-xx-xx`）只做“占位记录”，避免虚构细节

> 注意：仅依据提交信息与当前代码结构归纳，不对缺失信息进行臆测。如果你希望“更细粒度”的日志（例如列出具体函数/文件变化），可以在后续迭代中对关键提交补充 `git show <hash>` 的摘要。

---

## 1. 2026-01：Copilot 指引与工程维护

- 2026-01-27 `8343a264`：新增 `.github/copilot-instructions.md`（为 AI 编码代理提供项目规范）。
- 2026-01-02 `83329879`：合并远端 dev 分支。
- 2025-12-27 `969edf3c`：一次变更记录（提交信息为 change）。

里程碑解读：

- 文档化与工程治理加强（为后续自动化协作/持续迭代提供规范）。

## 2. 2025-12：实验程序与配置优化、级联失效模型封装

- 2025-12-04 `026c520d`：优化实验程序与配置文件。
- 2025-12-04 `62a62e00` / `72247b98`：`operator.py` 多次调整（实验安装/管理/导入流程）。
- 2025-12-04 `54915355`：`simulator.py` 更新子进程执行方式，改用 `sys.executable`（提高跨环境一致性）。
- 2025-12-04 `7f5dc8f2`：更新 `pyproject.toml`、`README.md`、`setup.py`（打包与说明同步）。
- 2025-12-14 `1042f043`：示例程序 IB2111 集成模型/过程/算法等结构。
- 2025-12-14 `126a1b91`：`set_parameters_variables.py` 调整。
- 2025-12-14 `252574cb` / `a1dd2292`：`set_config_variables.py` 调整。
- 2025-12-14 `572c5389`：更新版本号。
- 2025-12-14 `82547d3b`：新增示例模型用于调用封装的级联失效模型。
- 2025-12-14 `fbcee184`：开发封装的级联失效模型。
- 2025-12-18 `917b0b91`：尝试将封装的网络级联失效模型嵌入模拟器。

里程碑解读：

- **入口运行方式**更稳健（解释器选择问题被修复）。
- **Operator/配置**继续固化“库文件夹 → 输出目录副本 → 动态导入”的运行范式。
- 引入/推进“级联失效模型”封装，表明模型库开始模块化与可复用。

## 3. 2025-07 ~ 2025-10：依赖与管理维护

- 2025-07-05 `bdfb5e75`：奖励函数参数调整（调试期）。
- 2025-10-29 `ed98a68e`：管理工具包列表。

里程碑解读：

- 强化学习相关实验存在调参/调试阶段提交。
- 依赖管理持续演进。

## 4. 2025-05 ~ 2025-06：RL 收敛曲线、Collector 与实验组管理优化

该阶段提交较多，主题聚焦于：

### 4.1 强化学习与收敛曲线可视化

- 2025-05-21：静态方法绘制收敛曲线、支持滑动平均、保存路径调整。
- 2025-05-25：收敛曲线绘制函数从 `rl_utils.py` 移动到 `visualization_tools.py`（职责更合理）。
- 2025-05-26：修复中英文字体混编错误。

### 4.2 Collector 数据收集与导出

- 2025-05-24 ~ 2025-05-27：collector 反复优化、修复字典更新错误、支持导出 Excel。

### 4.3 实验组运行逻辑与配置

- 2025-06：修复 RL 输出目录处理、优化实验管理配置、修正输出路径、简化运行方式设定逻辑。

里程碑解读：

- 从“能跑”转向“可分析、可复现”：收敛曲线与导出体系开始稳定。
- 运行与输出目录结构在 RL 与 ABM 并存的情况下逐渐收敛。

## 5. 2024-12：风险敞口估算与相关工具函数强化

从提交信息看，该阶段主要围绕“银行间双边敞口估算（中心-外围/中心-边缘等）”与工具函数 `fun_calibrate_interbank_exposure.py` 多次迭代。

- 2024-12-13 ~ 2024-12-31：大量 `fun_calibrate_interbank_exposure.py` 的 `mod` 提交、以及算法尝试/重构。

里程碑解读：

- 风险敞口矩阵/校准工具是模型输入与初始化的重要环节。
- 建议后续将该部分从“日志式提交”进一步整理为：
  - 算法说明（输入/输出/假设）
  - 单元测试/基准数据
  - 示例脚本

---

## 6. 待补充（建议你确认后我可继续自动补齐）

1. **main 分支开发日志**：当前工作区处于 `dev` 分支；如果你希望“按 main 分支”补全，需要本地存在 `main` 分支或能检出 `origin/main`，我可以在不联网的前提下直接切换并重新生成。
2. 对于大量 `mod 202x-xx-xx` 类型提交：可挑选每个时间段 3~5 个关键 commit，让我用 `git show <hash>` 提取文件列表/核心 diff 摘要，从而把日志写得更“可读且可追溯”。
